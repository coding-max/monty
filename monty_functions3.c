#include "monty.h"

/**
 * monty_push - pushes an element to the stack.
 * @top_ptr: idk.
 * @line_n: idk.
 */
void monty_nop(stack_t **top_ptr, unsigned int line_n)
{
	*top_ptr = *top_ptr;
	line_n = line_n;
}